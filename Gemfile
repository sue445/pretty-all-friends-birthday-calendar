# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "hashie"
gem "icalendar", ">= 2.11.0"
gem "rake", require: false

group :test do
  gem "rspec"
  gem "rspec-temp_dir"
end
